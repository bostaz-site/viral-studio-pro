[build]
builder = "DOCKERFILE"
dockerfilePath = "vps/Dockerfile"
dockerContext = "vps"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
